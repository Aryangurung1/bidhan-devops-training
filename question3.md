1. Run app developed in previous classes using docker compose(use all necessary docker compose component like volume ,secrets , network,CPU/Memory,user,healthcheck,set variables with interpolation,additional_contexts,action= sync,restart= always etc)
2. Use Monitoring tool to view Real-time docker logs in the browser
